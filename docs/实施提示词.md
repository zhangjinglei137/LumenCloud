# 实施提示词（切换新窗口用）

> 用法：把「主提示词」贴到新窗口开始；每批完成后，把该批末尾「👉 下一批提示词」整段复制贴给助手，自动续接下一批。
> 文档基准：`docs/审查-待办清单.md`（统一批次）、`docs/审查-用户反馈12问题.md`（Q1-Q12 详细）、`docs/审查-架构与设计逻辑.md`、`docs/审查-代码细节优化.md`、`docs/审查-文档与流程.md`

---

## 主提示词（第 1 次粘贴）

```
你现在接手 LumenCloud（/home/zxl/workspace/zxl/LumenCloud）的实施阶段。
项目背景：拾光云映，智能影视下载管理系统（FastAPI + Vue3 + PostgreSQL 16 + 7 个后台任务 + Docker Compose）。
已完成两轮 council 审查，全部结论在 docs/ 下 5 份「审查-*.md」，其中「审查-待办清单.md」是统一实施依据。

【第一步：必读】
1. docs/审查-待办清单.md —— 统一修复批次（第0~4批）、P1 清单、验收口径
2. docs/审查-用户反馈12问题.md —— Q1-Q12 详细根因/定位/建议（第0批的 Q1 也在这里）
3. 按需读 docs/审查-架构与设计逻辑.md、审查-代码细节优化.md、审查-文档与流程.md

【实施纪律（严格遵守）】
- 全程中文回复
- 先读后改：修改前先 Read/查看目标文件与行号，不臆测
- 不得启动/停止/重启任何服务（数据库、开发服务器、应用进程等）。如需启动服务验证，先问我
- 破坏性操作（删除/覆盖/重命名/清空、DB 数据变更）前说明影响并等我确认
- 同一操作连续失败 2 次时停止，报告已尝试方案与失败原因，等我指示
- 未实际执行验证的不得声称已完成；只陈述真实执行的结果
- 我只在明确要求时才执行 git commit（提交信息用中文，Conventional Commits 结构）
- 测试：backend/ 用 pytest（backend/tests/）；改后端先看相关测试是否需更新

【验证口径（每批通用）】
- 改完跑相关单测：`cd backend && python -m pytest tests/ -x -q`（若部分失败，先修再继续）
- 无法自动验证的（涉及外部服务如 alist/aria2/Emby），明确列出「需联调验证项」留给我

【当前任务：第 0 批 —— 配置基线（Q1）】
目标：修复转存链路根因配置，为后续所有回归验证打基线。
详见 docs/审查-用户反馈12问题.md §Q1（P1 配置类：quark_default_folder ≠ alist Quark 驱动 root_folder_id）。

请：
1. 先读 transfer.py:104-140（_get_link_wait_visible）与 transfer.py:770-778（save folderId）、alist.py:117-131（get_link）确认现状
2. 设计并实现「设置页加『验证 folderId』按钮」：点击调用一次 alist /api/fs/get 探测 /quark 下文件可见性并返回诊断（参考 Q1 建议第 2 点）
3. 运维手册补排障条目「转存无 success → 先验 quark_default_folder 是否等于 alist Quark 驱动 root_folder_id」（写进 docs/运维手册.md）
4. 说明：实际修正配置值需要你在服务器查 alist GET /api/admin/storage/list 里的 Quark root_folder_id —— 把查询方法写进运维手册即可，真实改配置留给我
5. 完成后跑 pytest 回归，报告改动清单 + 待我配置验证项

完成标准：验证按钮可用 + 运维手册条目存在 + pytest 通过。

👉 下一批提示词：
```

---

## 第 1 批提示词（核心链路 bug，依赖第 0 批 Q1 基线回归）

```
继续实施 LumenCloud，进入第 1 批：核心链路 bug（以 Q1 配置修复为基线回归）。
文档：docs/审查-待办清单.md §三 第1批；详细见 docs/审查-架构与设计逻辑.md。

本批 4 项（全部 P1，均有精确行号与建议，先读后改）：
1. A-1 / Q8②③ 转存续跑闭环：transfer.py 步骤 6（约 893-933）成功事务提交后补 _spawn(process_transfer_queue)，与失败回退（:890）对称；_process_lock 保证串行。读 scan.py:923-924、transfer.py:352-355 确认联动
2. B-3 recovery 副作用顺序：recovery.py 阶段②的 aria2.remove 移到阶段③ CAS 成功（rowcount==1）之后执行（读 recovery.py:125-182）
3. B-1 paused 死锁：transfer.py:491-519 _sync_media_status 的 WHERE 扩展为 status in ('downloading','paused')，或 media.py PATCH 拒绝有进行中任务时设 paused（读 media.py:25）
4. B-2 delete 竞态：media.py:367-399 delete_media 先获取 _scan_locks[media_id]（或 _enqueue 校验 media 存在）；可选给子表 FK 加 ON DELETE CASCADE

规则：先读每个目标函数完整代码再改；改一个跑一次相关测试（backend/tests/test_transfer.py、test_scan_*.py、test_council_fixes.py、test_oracle_fixes.py 等）；保持既有条件更新/CAS 风格一致；不改动 Q1 配置值本身。
完成后报告：每项改动点 + 对应测试结果 + 需联调验证项。

👉 下一批提示词：
```

---

## 第 2 批提示词（数据与容量正确性）

```
继续实施 LumenCloud，进入第 2 批：数据与容量正确性。
文档：docs/审查-待办清单.md §三 第2批；详细见 docs/审查-用户反馈12问题.md、docs/审查-代码细节优化.md §I。

本批 4 项（P1）：
1. Q3 toFixed 崩溃（含 C-3）：frontend/src/utils/format.ts 的 formatGb/formatBytes 加 typeof === 'number' 守卫（或 Number() 转换）；后端 routers/capacity.py unavailable 分支返回 used_gb:None, total_gb:None；顺带把 /api/capacity 改为复用模块级单例 provider（services/capacity.py 已有模块级单例）
2. C-2 alist 分页：services/alist.py:83-89 list_dir 实现分页循环（page=1,2,... 直到返回 < per_page），防容量统计低估
3. Q2① 审批去重：approvals.py create_approval + media.py create_media 写入前查 Media.tmdb_id（非空时）命中 409「已在影视库」；可选 Media.tmdb_id 加 UNIQUE 约束（先清理重复，加 Alembic 迁移）
4. C-1 Emby aired-only：services/emby.py:160-164 get_missing_episodes 返回前按 PremiereDate <= now 过滤未播出集（或 scan._emby_missing_codes 层过滤）—— 为 Q12 前置

注意：Q2① 若加 UNIQUE 约束涉及 Alembic 迁移（backend/alembic/），新增迁移文件，勿改历史迁移。
改后跑 pytest（重点 test_capacity.py、test_transfer.py、test_scan_baseline.py）；前端改动用 vue-tsc/构建检查（npm run build 或 ts-check）。
完成后报告改动清单 + 测试结果。

👉 下一批提示词：
```

---

## 第 3 批提示词（接口与调度配置）

```
继续实施 LumenCloud，进入第 3 批：接口与调度配置。
文档：docs/审查-待办清单.md §三 第3批；详细见 docs/审查-架构与设计逻辑.md、docs/审查-代码细节优化.md。

本批 2 项（P1）：
1. E-1 scan 异步化：media.py:419 与 approvals.py:169 的同步 await run_scan / scan_media 改 fire-and-forget（参考 scan.py 内部 _trigger_transfer 模式），立即返回 task_run_id，前端轮询 /api/logs 获取结果
2. A-2 双层开关统一：scheduler.py 的 _JOB_DEFAULT_ENABLED 全 False 与总开关语义统一（三选一，推荐②：scheduler_enabled=true 时 job 级默认也开启；或①前端补 job 级开关 UI + settingsMeta.ts 增 scheduler.* 动态键 + 运维手册补启用步骤）

注意：E-1 改接口契约会影响前端（media 详情页/审批页调 scan 后的处理），同步检查 frontend/src 相关调用。
改后跑 pytest + 前端构建检查。
完成后报告改动清单 + 测试结果 + 需联调验证项。

👉 下一批提示词：
```

---

## 第 4 批提示词（P2 批量，按耦合分组）

```
继续实施 LumenCloud，进入第 4 批：P2 批量（按耦合分组逐组做，每组完成后可暂停让我确认）。
文档：docs/审查-待办清单.md §三 第4批 + 详细条目定位。

组 A（同模块 task_run）：
- D-1 task_run/quark_capacity_log 清理 job（保留 30 天；参考 cleanup.py 模式新增或扩展）
- Q8① record_task_run 加 duration_seconds 真实耗时（tasks/__init__.py:39-58 + 各 job 入口 time.monotonic()）

组 B（JOIN users，复用同一模式）：
- Q10 消息/审批显示用户名：notifications.py _notif_dto、approvals.py _wr_dto LEFT JOIN users 返回 *_username；前端改显示
- Q6 邀请码：admin.py list_invites JOIN users 返回 used_by_username；SettingsView 加复制按钮 + 复制注册链接（/register?code=）；RegisterView 读 ?code= 预填；used_at 用 formatTime

组 C（Emby 功能，依赖 C-1 已修）：
- Q12 SeriesStatus：emby.py Fields 加 SeriesStatus + _normalize_library_item 返回 series_status；Media 表加 series_status 列（Alembic 迁移）；Emby 库卡片 in_media=false 时加「加入订阅」按钮复用 POST /api/media

组 D（配置显示）：
- Q5 jwt_secret：settings.py _services_configured 对 jwt_secret 改按文件存在性判定（或剔除）；SettingsView 系统参数区改名「业务参数」+ 加只读说明；联动 docs G-2 部署状态卡
- Q2② 审批刷新双保险：ApprovalsView onApprove/onReject 成功后显式再调 store.fetchList()；确认后端 approve 事务提交后才返回

组 E（纯前端，可并行）：
- Q4 凭据页改版：SettingsView 双列 grid + 黄色叹号提示 + scan_baseline_required 改下拉中文「开启/关闭」
- Q7 容量显示：MainLayout el-progress circle width 30→40 + 百分比文字
- Q9 主题切换：CSS 变量 --lc-* 收敛到 :root/[data-theme] + 切换开关 + localStorage 持久化
- Q11 用户管理：新增 routers/admin users API（列表/改role/删除含保护）+ UsersView + 菜单（admin 可见）

组 F（零散独立）：
- D-1 serverId：emby.py emby_web_url 补 &serverId=，GET {base}/System/Info/Public 取 Id + 模块级缓存 + 获取失败降级 emby_web_url=None（前端隐藏入口）

每组完成后：跑 pytest + 前端构建检查 + 报告该组改动清单。全部完成进入收尾。
👉 下一批提示词：
```

---

## 收尾提示词（最后粘贴）

```
LumenCloud 全部批次实施完成。现在做收尾：
1. 通读 docs/审查-待办清单.md，逐条核对已实施/未实施状态，输出最终对照表（编号 / 状态 / 改动文件）
2. 同步更新 docs/ 下审查文档中「已修复」标记（如需，新增一列或标注状态），确保文档与代码一致
3. 更新 docs/运维手册.md：补充本批新增的排障条目（转存无 success、jwt_secret 说明、task_run 清理指引等，若前批未补完）
4. 运行完整测试套件：cd backend && python -m pytest tests/ -q，报告结果
5. 输出「需用户手动操作项」清单（如：Q1 配置 root_folder_id 实际值、需联调的外部服务验证项、DB 迁移执行确认）
6. 若需要，汇总一份本阶段变更摘要（中文，可作 commit message 素材；是否 commit 由我决定）
完成后等待我指示。
```
