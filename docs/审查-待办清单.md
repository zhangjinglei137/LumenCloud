# 审查报告 ④ 待办清单（两轮合并最终版，P0/P1/P2 汇总）

> 审查基准：成熟自用产品化
> 审查方式：三方 councillor（ds / glm / qw）独立审读 + council 共识裁决（两轮）
> 统计：**第一轮 39 条 + 第二轮 14 条（对应 12 个用户问题）** —— **P0 = 0，P1 = 11，P2 = 其余**
> 关联文档：
> - `审查-架构与设计逻辑.md`（A/B/C/D/E）
> - `审查-文档与流程.md`（G）
> - `审查-代码细节优化.md`（D/E/F/H/I）
> - `审查-用户反馈12问题.md`（Q1-Q12，第二轮详细分析）
> 本清单为实施依据，各条目可直接作为任务来源；实施需等用户通知后另行执行。
>
> **实施状态（2026-09 收尾更新）**：✅ = 已实施；🔶 = 部分实施（代码完成，配置/文档类待用户或收尾补完）；⬜ = 未实施。
> 详细改动文件见第三/四部分标注与本仓库近期 commit/工作树 diff。

---

## 一、P0 结论：0 个

依据（两轮 council 共识）：
1. **无数据丢失/损坏路径**——最接近的是 B-3（aria2 任务被提前移除），但 es/tq 记录保留，下一轮 recovery 可恢复；
2. **无凭据泄露攻击面**——DB 弱密码 `"lumencloud"` 仅存在于内网 compose，属 P2 加固项；
3. **无完全不可用路径**——A-1 队列积压属功能未按设计运行，兜底 job 与手工触发仍可用；
4. **两轮三席位均无 P0**，最高定级 P1（含 1 项配置类 P1）。

---

## 二、P1 问题清单（11 项）

### 第一轮 P1（8 项，council 共识）

| 序 | 编号 | 问题 | 定位 | 触发后果 | 实施状态 |
|----|------|------|------|----------|----------|
| 1 | **A-1** | 转存队列缺「完成后续跑」闭环 | `tasks/transfer.py:893-933`、`:890`、`:352-355`；`tasks/scan.py:923-924` | 队列静默积压、容量恢复不自动重试（Q8②③ 叠加因素） | ✅ 第1批：transfer.py 步骤6 成功事务后 + `_complete_download` 释放容量后补 `_spawn(process_transfer_queue)` |
| 2 | **B-1** | `media.status='paused'` 进行中被设置 → 永久死锁 downloading | `tasks/transfer.py:491-519`；`routers/media.py:25` | media 永久 downloading，需人工介入 | ✅ 第1批（采用「PATCH 拒绝」方案）：media.py `_has_in_progress_tasks` + 设 paused 时有进行中任务 409 |
| 3 | **B-3** | recovery 阶段② aria2 副作用在 CAS 失败时不回滚 | `tasks/recovery.py:125-182` | aria2 任务已移除但状态卡 downloading 约 2h | ✅ 第1批：recovery.py aria2.remove 移到阶段③ CAS 成功（rowcount==1）之后 |
| 4 | **B-2** | `DELETE /api/media/{id}` 与 scan 入队竞态 | `routers/media.py:367-399` | 孤儿子表数据 + 转存 CAS 空转 | ✅ 第1批：delete_media 持 `app.tasks.scan._media_lock(media_id)`（检查+删除+commit 同锁）；FK CASCADE 未做（缓解项） |
| 5 | **C-1** | Emby 遗漏集含未播出集 → 误入队 | `services/emby.py:160-164` | 追更场景必触发，下载预告/空文件至 failed（Q12 前置） | ✅ 第2批：emby.py `Fields=PremiereDate` + `PremiereDate > now` 过滤（缺失/解析失败保守保留） |
| 6 | **C-2** | alist `list_dir` 不分页（per_page=1000） | `services/alist.py:83-89` | 容量统计低估，可能突破夸克硬上限 | ✅ 第2批：alist.py list_dir 分页循环（page→短页停止，2000 页上限防御） |
| 7 | **E-1** | `scan_media` 同步 await 阻塞 HTTP → 504 | `routers/media.py:419`；`routers/approvals.py:169` | 巡检数分钟阻塞，超时后状态不一致 | ✅ 第3批：scan.py `trigger_scan_background` fire-and-forget；media.py/approvals.py 立即返回 `task_run_id: null`；前端轮询 /api/logs |
| 8 | **A-2** | 定时任务「双层开关」配置断层 | `scheduler.py:56-65`、`:88-100`；`settings.py:56`；`settingsMeta.ts:181-185` | 开总开关实际全不运行，放大 A-1 静默积压 | ✅ 第3批（方案②）：scheduler.py job 级未配置跟随总开关（删除 `_JOB_DEFAULT_ENABLED`）；运维手册 §6.5 补排障行 |
| 9 | **Q1** | 转存失败：落盘超时 300s（配置类 P1） | `tasks/transfer.py:104-140`、`:770-778`；`services/alist.py:117-131` | **核心链路全断**：无 success、aria2 无任务（Q8②③ 根因） | 🔶 第0批：诊断接口 `POST /api/settings/verify-quark` + 设置页「验证 folderId」按钮 + 运维手册 §6.4 排障条目；**实际配置值修正待用户**（alist root_folder_id 比对填入） |
| 10 | **Q2①** | 想看审批已存在 TMDBID 未去重 | `routers/approvals.py:75-109`；`routers/media.py:227-248`；`models/__init__.py:48-74` | 重复 Media 记录 + 重复转存下载（应用层+DB 层双重无兜底） | ✅ 第2批：create_approval/approve_approval/create_media 三入口 409 查重 + `Media.tmdb_id` UNIQUE（迁移 0006） |
| 11 | **Q3** | 队列页崩溃：`n.toFixed is not a function` | `frontend/src/utils/format.ts:3-7,16-23`；`QueueView.vue:123,127,131,172,272` | 整页渲染崩溃、列表瞬间消失（必现白屏） | ✅ 第2批：format.ts `typeof` 守卫；capacity.py unavailable 显式 null + 复用模块级单例 provider（C-3） |
| 12 | **D-1** | Emby 跳转播放 URL 缺 `serverId` → 空白页（用户实测） | `services/emby.py:229` | 影视库「跳转播放」必现空白页 | ✅ 第4批组F：`_get_server_id()`（/System/Info/Public 惰性缓存）+ emby_web_url 补 `&serverId=` + 失败降级 None（前端隐藏入口） |

> 说明：Q8②③（转存无 success + aria2 无到达）**已裁决合并进 A-1**（根因=Q1 配置，A-1/A-2 叠加加剧），不单独计 P1。

---

## 三、统一修复批次（按依赖分组）

### 第 0 批：配置基线（阻塞后续回归验证）
1. **Q1**（配置 P1）：比对 `quark_default_folder` 与 alist Quark 驱动 `root_folder_id` 修正配置；设置页加「验证 folderId」按钮；运维手册补排障条目 — 🔶 代码侧完成（第0批：`alist.py diagnose_quark_mount` + `POST /api/settings/verify-quark` + 设置页按钮 + 运维手册 §6.4）；配置实际值修正待用户

### 第 1 批：核心链路 bug（以 Q1 修复为基线回归）
2. **A-1 / Q8②③**（P1）：步骤 6 成功事务提交后 `_spawn(process_transfer_queue)`（`_process_lock` 保证串行）— ✅ `tasks/transfer.py`（步骤6 成功后 + `_complete_download` 释放容量后两处续跑）
3. **B-3**（P1）：`aria2.remove` 移到 CAS 成功（rowcount==1）之后 — ✅ `tasks/recovery.py`
4. **B-1**（P1）：`_sync_media_status` WHERE 扩展为 `status in ('downloading','paused')`，或 PATCH 拒绝有进行中任务时设 paused — ✅ 采用后者：`routers/media.py`（`_has_in_progress_tasks` + 409）
5. **B-2**（P1）：`delete_media` 获取 `_scan_locks[media_id]` 或 `_enqueue` 校验 media 存在；FK 加 ON DELETE CASCADE 兜底 — ✅ `routers/media.py`（持 `_media_lock`）；FK CASCADE 未做（锁修复已关闭窗口）

### 第 2 批：数据与容量正确性
6. **Q3**（P1，含 C-3）：前端 `formatGb/formatBytes` 加 `typeof === 'number'` 守卫（可独立先行）；后端 capacity unavailable 返回 null + 复用模块级单例 provider — ✅ `format.ts`；`routers/capacity.py`（显式 null + 单例 `provider`）
7. **C-2**（P1）：alist `list_dir` 分页循环 — ✅ `services/alist.py`
8. **Q2①**（P1）：审批/添加影视 tmdb_id 去重 + `Media.tmdb_id` UNIQUE 约束 — ✅ 三入口 409（`approvals.py`/`media.py`）+ `models` unique + 迁移 0006
9. **C-1**（P1）：Emby aired-only 过滤（`PremiereDate <= now`）—— **为 Q12 前置** — ✅ `services/emby.py`

### 第 3 批：接口与调度配置
10. **E-1**（P1）：scan 异步化（fire-and-forget + 前端轮询 `/api/logs`）— ✅ `tasks/scan.py`（`trigger_scan_background`）+ `media.py`/`approvals.py` + 前端 LogsView 轮询提示
11. **A-2**（P1）：双层开关语义统一 / 前端补 job 级开关 UI + 运维手册 — ✅ 方案②：`scheduler.py` job 级未配置跟随总开关；运维手册 §6.5 补排障行

### 第 4 批：P2 批量（按耦合分组）
12. D-1（task_run 清理）+ **Q8①**（耗时恒 0：record_task_run 加 duration）：同模块同批 — ✅ `cleanup.py prune_history_job` + scheduler `prune_history`（默认 30 天可配）+ `TaskRun.duration_seconds`（迁移 0007）+ 7 个 job 入口计时 + logs API/LogsView
13. **Q10 + Q6**（JOIN users 返回用户名 + 邀请码复制/注册链接/时间格式）：复用同一 JOIN 模式 — ✅ `approvals.py`/`notifications.py`/`admin.py` LEFT JOIN users（`*_username`）+ 前端（ApprovalsView/SettingsView 邀请码/RegisterView `?code=` 预填）
14. **Q12**（SeriesStatus 字段 + Emby 库「加入订阅」按钮 + Media.series_status 列）：依赖 C-1 先完成 — ✅ `services/emby.py` Fields+SeriesStatus + `Media.series_status`（迁移 0008）+ EmbyLibraryView 订阅按钮（series→tv 映射）
15. **Q5**（jwt_secret 判定修正 + 设置页只读说明）：联动 G-2 状态卡 — ✅ `routers/settings.py` 文件存在性判定 + SettingsView 只读说明/「业务参数」改名；G-2 状态卡见第四部分
16. **Q2②**（onApprove 双保险 fetchList + 后端事务时序确认）：需先复现确认根因 — ✅ 后端确认 commit 先于返回（approvals.py）；前端 onApprove/onReject 成功路径显式 `fetchList`
17. **D-1 serverId**（emby_web_url 补 `&serverId=`，`GET /System/Info/Public` 取 Id + 模块级缓存 + 失败降级 None）：独立零散项 — ✅ `services/emby.py` + 前端入口隐藏
18. Q4 / Q7 / Q9 / Q11（凭据页改版 / 容量显示 / 主题切换 / 用户管理）：可并行的纯前端工作 — ✅ SettingsView 双列+叹号+下拉；MainLayout 容量 40/5+百分比；主题 store+亮色切换+持久化+硬编码收敛；`/api/admin/users` 三端点 + UsersView/路由/菜单
19. G-1 文档补齐（定时启用清单、quark_quota_gb 落地、验收清单、task_run 清理指引、排障场景含 Q1/Q8/Q5、部署状态卡）：随各批同步更新 — 🔶 部分完成（G-6.1 第0批已补）；其余见第四部分（本次收尾统一补齐）

---

## 四、文档/流程待办（独立于代码，可先行）

| 编号 | 事项 | 优先级 | 实施状态（收尾更新） |
|------|------|--------|----------------------|
| G-1.1 | 定时任务启用步骤清单（含 job 级开关） | 高（联动 A-2） | ✅ 运维手册 §6.4「定时任务启用与开关」 |
| G-1.2 | `quark_quota_gb=210` 落地位置提示 | 高（联动 C-2，防止误拒大文件） | ✅ 运维手册 §6.4（quark_quota_gb 键 + 设置页业务参数） |
| G-6.1 | 转存无 success 排障条目（先验 folderId，Q1/Q8） | 高 | ✅ 运维手册 §6.4「转存无 success / 等待落盘超时 → 先验 folderId」（第0批已补） |
| G-6.2 | jwt_secret 说明（自动生成/轮换/与密码无关，Q5） | 中 | ✅ 运维手册 §6.4「jwt_secret 与登录凭据说明」（设置页亦有只读说明） |
| G-1.5 | 排障场景补充：备份验证、paused 死锁处置、JWT 轮换、alembic 失败处置 | 中 | ✅ 运维手册 §6.5（paused 处置 / JWT 轮换 / alembic 失败 / 备份验证） |
| G-1.4 | task_run / quark_capacity_log 清理指引 | 中 | ✅ 运维手册 §6.4（prune_history + `task_run_retention_days`） |
| G-1.3 | 端到端验收清单（修复完成后建） | 中 | ✅ 运维手册 §6.6「修复后端到端验收清单」 |
| G-2 | 部署状态卡（system_config 生效键值） | 中 | ✅ 运维手册 §6.6「部署状态卡（system_config 生效键值）」 |
| G-3 | 设计文档与代码双向同步（job 数量、§4.4、公式） | 中 | 🔶 部分（scheduler docstring 已同步 8 job；容量公式/通知间隔待真实环境核对） |
| G-6.3 | 设置页邀请码 UX 说明（复制/注册链接/用户名/时间，Q6） | 低 | ✅ 运维手册 §6.4（邀请码 UX 与注册链接） |
| G-4 | 通知事件触发表 | 低 | ⬜ 未实施（建议另行，非阻塞） |
| G-5 | recovery 行为说明（频率/阈值/副作用） | 中 | ⬜ 未实施（建议另行，非阻塞；行为已在代码注释中说明） |

---

## 五、待确认项（修复时验证，非缺陷）

1. **Q2② 用户实测不刷新的底层原因**：竞态 / 时序 / 后端读一致性三选一，需复现后确认。
2. **get_link 失败的分支归属**：终态失败 vs 非终态回退（决定 `transfer.py:890` 续跑是否触发，影响修复后回归预期）。
3. **T1**：cloudsaver `share-info` 不传 `receive_code`，有提取码的夸克分享是否失败——需真实提取码分享实测。
4. **T2**：`transfer.py:116` 文件名匹配可能被夸克同名旧文件误导（cleanup 未运行时）——需实测确认。
5. **B-4**：步骤 6 无 rowcount 校验的竞态实际触发概率（已裁决 P2，理论窗口极小）。
6. **G-3**：容量安全余量公式、`notification_scan` 间隔的设计值与实现关系。

---

## 六、达成方式说明

- **审查基准**：成熟自用产品化（长期运行 / 可能给别人用 / 数据不能丢 / 出问题能快速定位），未套用团队协作/多租户/SLA/组织合规等企业组织级要求。
- **证据颗粒度**：P0/P1 均含精确 `文件:行号` + 代码摘录 + 触发路径 + 建议；P2 以定位 + 建议为主。
- **两轮结构**：第一轮 council 全量审查（39 条，架构/文档/代码三主题）；第二轮用户反馈 12 问题（Q1-Q12，14 条目，含 3 项新 P1 与 1 项合并）。
- **本清单为实施依据**：各条目可直接作为后续实施时的任务来源；实施需等通知后另行执行。
