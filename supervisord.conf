# ============================================
# LumenCloud 进程管理：仅启动 uvicorn 单进程
# 调度器（APScheduler）内嵌于 uvicorn 进程，
# 不设独立 worker，避免多进程共享 SQLite 锁冲突
# ============================================

[supervisord]
nodaemon=true
logfile=/dev/null
logfile_maxbytes=0
pidfile=/tmp/supervisord.pid

[program:lumencloud]
directory=/app/backend
command=uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
# 非 root 运行：与 Dockerfile 创建的 lumencloud 用户（UID 1000）一致
user=lumencloud
autostart=true
autorestart=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0